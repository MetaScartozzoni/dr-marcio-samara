# 🏥 Portal Dr. Marcio - Configuração Apache ROBUSTA
# Otimizada para Hostinger - Resolução de CSP

# =====================================================
# 🔒 CONTENT SECURITY POLICY - VERSÃO ROBUSTA
# =====================================================

# Habilitação do módulo de rewrite
RewriteEngine On

# Configuração robusta de CSP para resolver todos os problemas
<IfModule mod_headers.c>
    # CSP Detalhado - Permitir TODOS os recursos necessários
    Header always set Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://unpkg.com https://cdn.jsdelivr.net https://cdnjs.cloudflare.com https://js.stripe.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com https://cdnjs.cloudflare.com https://fonts.gstatic.com; font-src 'self' https://fonts.gstatic.com https://fonts.googleapis.com data:; img-src 'self' https: data: blob:; connect-src 'self' https://*.supabase.co https://obohdaxvawmjhxsjgikp.supabase.co wss://*.supabase.co; frame-src 'self' https://js.stripe.com https://www.google.com; worker-src 'self' blob:; object-src 'none'; base-uri 'self';"
    
    # Headers de Segurança
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
    Header always set Referrer-Policy "strict-origin-when-cross-origin"
</IfModule>

# =====================================================
# 🌐 REDIRECIONAMENTO HTTPS FORÇADO
# =====================================================

# Redirecionar HTTP para HTTPS
RewriteCond %{HTTPS} off
RewriteCond %{HTTP_HOST} !^localhost [NC]
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]

# =====================================================
# 📄 PÁGINAS DE ERRO PERSONALIZADAS
# =====================================================

ErrorDocument 404 /404.html
ErrorDocument 500 /500.html
ErrorDocument 403 /404.html

# =====================================================
# 🚀 OTIMIZAÇÃO DE PERFORMANCE
# =====================================================

# Cache para arquivos estáticos
<IfModule mod_expires.c>
    ExpiresActive On
    
    # Cache de imagens (1 mês)
    ExpiresByType image/jpg "access plus 1 month"
    ExpiresByType image/jpeg "access plus 1 month"
    ExpiresByType image/gif "access plus 1 month"
    ExpiresByType image/png "access plus 1 month"
    ExpiresByType image/webp "access plus 1 month"
    ExpiresByType image/svg+xml "access plus 1 month"
    ExpiresByType image/x-icon "access plus 1 year"
    
    # Cache de CSS e JS (1 semana)
    ExpiresByType text/css "access plus 1 week"
    ExpiresByType application/javascript "access plus 1 week"
    ExpiresByType text/javascript "access plus 1 week"
    
    # Cache de fontes (1 mês)
    ExpiresByType font/ttf "access plus 1 month"
    ExpiresByType font/woff "access plus 1 month"
    ExpiresByType font/woff2 "access plus 1 month"
    ExpiresByType application/font-woff "access plus 1 month"
    
    # HTML sem cache (sempre atualizado)
    ExpiresByType text/html "access plus 0 seconds"
</IfModule>

# Compressão GZIP para melhor performance
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
    AddOutputFilterByType DEFLATE image/svg+xml
</IfModule>

# =====================================================
# 🔐 SEGURANÇA ADICIONAL
# =====================================================

# Bloquear acesso a arquivos sensíveis
<Files "config.js">
    <RequireAll>
        Require all granted
    </RequireAll>
</Files>

<FilesMatch "\.(md|json|lock|log)$">
    <RequireAll>
        Require all denied
    </RequireAll>
</FilesMatch>

# Bloquear acesso às pastas de desenvolvimento
<IfModule mod_alias.c>
    RedirectMatch 403 /\..*$
    RedirectMatch 403 /node_modules/
    RedirectMatch 403 /supabase/
    RedirectMatch 403 /_lixeira/
    RedirectMatch 403 /integracoes/
</IfModule>

# =====================================================
# 📱 SUPORTE A PWA E MOBILE
# =====================================================

# Tipos MIME adequados
<IfModule mod_mime.c>
    # Webfonts
    AddType application/font-woff2 .woff2
    AddType application/font-woff .woff
    AddType application/font-ttf .ttf
    
    # Arquivos de manifesto
    AddType application/manifest+json .webmanifest
    AddType application/x-web-app-manifest+json .webapp
    AddType text/cache-manifest .appcache
</IfModule>

# =====================================================
# 🎯 URL REWRITE PARA SPA (SINGLE PAGE APP)
# =====================================================

# Suporte para rotas do frontend (se necessário)
<IfModule mod_rewrite.c>
    # Não reescrever arquivos e pastas que existem
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Não reescrever rotas de API ou assets
    RewriteCond %{REQUEST_URI} !^/api/
    RewriteCond %{REQUEST_URI} !^/assets/
    RewriteCond %{REQUEST_URI} !^/modules/
    
    # Para URLs sem extensão, redirecionar para index.html (SPA)
    # Descomente a linha abaixo se usar Single Page Application
    # RewriteRule ^.*$ /index.html [QSA,L]
</IfModule>

# =====================================================
# 🏆 OTIMIZAÇÃO FINAL
# =====================================================

# Remover assinatura do servidor (segurança)
ServerSignature Off

# Configuração de charset UTF-8
AddDefaultCharset UTF-8

# Fim da configuração robusta
