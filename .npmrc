# .npmrc
# Suppress development-only vulnerabilities that don't affect production builds
audit-level=moderate
# Ignore specific vulnerable packages that are only used in development
ignore-scripts=false
