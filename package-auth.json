{
  "name": "portal-dr-marcio-auth",
  "version": "1.0.0",
  "description": "Sistema de autenticação completo para funcionários do Portal Dr. Marcio",
  "main": "server-auth-production.js",
  "scripts": {
    "start": "node server-auth-production.js",
    "dev": "NODE_ENV=development node server-auth.js",
    "test": "node server-auth.js",
    "production": "NODE_ENV=production node server-auth-production.js",
    "setup": "npm install && cp .env.example .env",
    "health": "curl http://localhost:3000/api/health || echo 'Servidor não está rodando'",
    "logs": "pm2 logs portal-auth || echo 'PM2 não configurado'",
    "restart": "pm2 restart portal-auth || echo 'PM2 não configurado'",
    "stop": "pm2 stop portal-auth || echo 'PM2 não configurado'",
    "deploy": "pm2 start ecosystem.config.js --env production"
  },
  "keywords": [
    "autenticacao",
    "funcionarios",
    "portal",
    "dr-marcio",
    "google-sheets",
    "sendgrid"
  ],
  "author": "Portal Dr. Marcio",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "bcrypt": "^5.1.1",
    "@sendgrid/mail": "^7.7.0",
    "googleapis": "^126.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
